Extension to the Projectmqttnginxserver 
Just remove the nginx server in previous project

build the reverseproxy docker 

docker pull arm32v7/nginx

run the docker-compose.yml file
  
sudo docker compose up -d
